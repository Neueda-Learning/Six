---
name: payments-course-assistant
description: "Use when: 需要为本仓库的支付课程项目生成或修改 Spring Boot + Vue 代码、接口、数据库脚本、测试用例，且必须遵循既定状态机、幂等规则与错误码约定。"
---

# 1. Payments Course Assistant Skill

## 目标
为课程项目提供可直接落地的实现辅助，确保所有输出与仓库中的支付设计文档一致。

## 触发场景
- 生成后端基础代码：实体、DTO、Mapper、Service、Controller、异常处理。
- 生成前端页面：支付创建、列表筛选、详情与历史时间线。
- 生成数据库 DDL、索引、初始化数据。
- 编写接口联调示例、Postman 样例、基础测试用例。

## 必须遵守的业务约束
- 仅使用状态：CREATED, VALIDATED, SENT, COMPLETED, FAILED。
- 仅允许以下状态转换：
1. CREATED -> VALIDATED
2. CREATED -> FAILED
3. VALIDATED -> SENT
4. VALIDATED -> FAILED
5. SENT -> COMPLETED
6. SENT -> FAILED
- 所有状态变化必须记录审计历史。
- 创建支付必须支持 idempotencyKey，并处理重复提交。

## 输出规范
- 代码输出优先最小可运行方案，不引入与作业无关的复杂组件。
- REST 接口必须包含：方法、路径、请求参数、响应体、错误码。
- 若新增字段或接口，必须提示同步更新：
1. 数据库结构
2. OpenAPI 注解
3. 前端 API 调用

## 后端实现清单（建议顺序）
1. 定义枚举：PaymentStatus, ErrorCode。
2. 定义实体：Payment, PaymentStatusHistory。
3. 定义请求响应 DTO。
4. 定义状态机校验组件。
5. 实现 PaymentService（幂等、校验、状态推进、历史记录）。
6. 实现 Controller 与全局异常处理。
7. 补充 OpenAPI 注解。

## 前端实现清单（建议顺序）
1. API 模块：payment.js。
2. 页面：PaymentCreate, PaymentList, PaymentDetail等。
3. 列表筛选与分页。
4. 详情页历史时间线与失败错误展示。
5. 统一错误提示与状态颜色映射。

## 验收检查
- 是否保留完整 audit trail。
- 是否覆盖关键异常：非法流转、校验失败、支付不存在、网络模拟失败。
- 是否能通过 Happy Path：CREATED -> VALIDATED -> SENT -> COMPLETED。


# 2.行为准则：减少LLM常见编码错误

**权衡：** 这些准则偏向谨慎，而非速度。对琐碎任务，请自行判断。

## 先思考，后编码

**不要臆断。不要掩饰疑惑。暴露权衡。**

实现之前：
- 明确陈述你的假设。如有不确定，请提问。
- 若存在多种解释，请列出它们——不要擅自默认选一个。
- 若有更简单的方案，请说明。在有充分理由时，可提出反对意见。
- 若有不清晰之处，停下来。指出困惑之处并提问。

## 简单至上

**用最少的代码解决问题。不做投机性编写。**

- 不添加未被要求的功能。
- 不为只用一次的代码做抽象。
- 不添加未被要求的“灵活性”或“可配置性”。
- 不为不可能发生的场景做错误处理。
- 如果你写了200行，而本可以只写50行，那就重写。

问问自己：“资深工程师会觉得这写复杂了吗？” 如果是，就简化。

## 手术式修改

**只触碰必须改的地方。只清理自己造成的混乱。**

编辑现有代码时：
- 不要“改进”周围的代码、注释或格式。
- 不要重构没有问题的东西。
- 尽量遵循现有风格，即使你个人会采用不同方式。
- 若发现无关的、无用的代码，请指出来——不要直接删除它们。

当你的修改产生“孤儿”代码时：
- 移除因你改动而不再使用的导入/变量/函数。
- 除非被要求，否则不要移除之前已经不存在的、无用的代码。。

检验标准：每一处更改都应直接与用户的请求相关联。

## 目标驱动执行

**定义成功标准。循环执行，直到验证通过。**

将任务转化为可验证的目标：
- “增加校验” → “为无效输入编写测试，然后让测试通过”
- “修复Bug” → “编写能复现该Bug的测试，然后让测试通过”
- “重构X” → “确保重构前后测试均通过”

对于多步骤任务，简述计划：
```
1. [Step] → verify: [check]
2. [Step] → verify: [check]
3. [Step] → verify: [check]
```