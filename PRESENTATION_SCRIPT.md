# 答辩演讲稿 / Presentation Script

配合 [Payments-Processing-Presentation.pptx](Payments-Processing-Presentation.pptx)（16 页）使用，按 [payment_processing.md](../Downloads/payment_processing.md) 建议的展示流程编写。全文预计朗读时长 12-15 分钟，可根据实际分配时间（团队 15-20 分钟）灵活删减。

[TOC]

---

## Slide 1：封面

> 大家好，我叫 [你的名字]，接下来由我介绍我们团队的课程项目——Payments Processing System，一个支付处理生命周期管理系统。这个项目用 Spring Boot 3 做后端，Vue 3 做前端，支持中、英、德三语言切换。

## Slide 2：目录 Agenda

> 我今天的介绍分七个部分：项目背景与目标、状态机设计、系统架构、核心功能演示、我个人负责实现的五个功能模块、开发过程中遇到的挑战，以及后续的改进计划。

## Slide 3：项目背景与目标

> 这个项目的目标是设计并实现一个处理金融支付完整生命周期的系统——从创建、验证、发送到完成或失败，并且要保留完整的状态变更审计记录。课程要求里明确说明：不需要做用户认证，假设只有单一用户；也不需要对接真实的支付网关，内部模拟处理过程即可。技术栈是我们团队自主选择的，我们选了 Spring Boot 加 Vue。

## Slide 4：支付生命周期与状态机设计

> 支付有五种状态：CREATED、VALIDATED、SENT、COMPLETED，任意阶段都可以流转到 FAILED 这个终态。合法的流转路径由一个专门的 PaymentStateMachine 类集中维护，用一个 Map 存储"从哪个状态能到哪些状态"，从代码层面杜绝跳级或者逆向流转的可能。特别要说明的是，CREATED 到 VALIDATED 这一步，我们做了三层业务规则校验——金额、币种、账户，我后面负责的余额校验也加在了这一步。

## Slide 5：系统架构与技术栈

> 前端用 Vue 3 加 Element Plus 做 UI 组件库，路由用 vue-router，多语言用 vue-i18n。后端是标准的分层架构：Controller 负责接收请求，Service 处理核心业务逻辑，还有专门的 Validator 和 StateMachine 组件。所有接口统一返回 `{ success, data, errorCode, message }` 这样的结构，配合一个全局异常处理器，把各种业务异常统一转换成标准错误响应。另外我们还有一个定时任务，模拟真实网络处理场景下的异步状态推进。

## Slide 6：核心功能模块概览

> 简单过一下我们实现的功能：创建支付支持幂等键防止重复提交；可以查看支付详情和失败原因；有完整的状态历史时间线；支持按状态筛选和分页检索；还实现了一个回收站功能，软删除的记录会保留 30 天可以恢复，也可以永久删除。

---

## Slide 7：我的贡献（分割页）

> 接下来重点介绍五个由我独立设计并实现的功能模块。

## Slide 8：贡献 1 —— 多语言国际化

> 第一个是多语言支持。我引入了 vue-i18n，实现了中文、English、Deutsch 三种语言的实时切换。有几个我觉得比较有意思的细节：第一，首次打开页面时，我们会读取浏览器的系统语言自动匹配对应界面，而不是每次都先显示中文再手动切换；第二，切换语言后不只是我自己写的文案会变，Element Plus 组件库自带的分页器文案也会跟着联动切换，这是通过 `el-config-provider` 这个组件做全局配置注入实现的；第三，金额和日期会按照语言环境自动调整格式，比如英文用小数点、德文用逗号做小数分隔符；第四，错误码有一份前端本地化字典，会优先展示对应语言的错误描述。整个功能只改了前端代码，完全没有动后端接口。

## Slide 9：贡献 2 —— 自动状态推进与随机失败模拟

> 第二个是自动状态推进的随机失败模拟。项目里原本有一个定时任务，每隔几秒自动把待处理的支付往下推进一步，但问题是原来的逻辑必然会走到 COMPLETED，没办法演示失败分支。我给它加了一个可配置的失败概率，默认 20%，每次推进前会掷一个随机数，命中就直接转为 FAILED，并且根据当前所处的阶段选择比较贴近真实场景的错误码——比如在发送阶段失败更倾向于用网络超时错误。这样单次 20% 的概率下，一笔支付走完整个流程最终失败的概率大概是 49%，可以同时演示成功和失败两种真实分支。

## Slide 10：贡献 3 —— 账户余额充足性校验

> 第三个是账户余额校验，这是我独立设计实现的功能。需求很明确：只做余额是否充足的只读判断，不做真实扣款，不涉及并发扣减，账户余额从头到尾都不会变。我给 accounts 表加了一个 balance 字段，用了一个单独的迁移脚本，没有改动原有的 schema.sql 和 data.sql。核心校验方法只有一次查询，没有任何更新语句，这一点我在测试里也专门验证过。校验只在从 CREATED 到 VALIDATED 这一步触发，如果余额不足，即使调用方请求的是流转到 VALIDATED，实际执行的结果也会被强制改成 FAILED，返回 INSUFFICIENT_FUNDS 错误码——真实的业务规则会覆盖调用方传入的参数。这段逻辑在手动状态流转接口和自动定时任务两个入口是共用同一套判断代码的。

## Slide 11：贡献 4 —— 列表页与详情页自动轮询

> 第四个是前端的自动轮询。因为后端定时任务每 5 秒才推进一次状态，如果前端页面不主动刷新，用户会看到状态好像"卡住了"。详情页原本已经有轮询逻辑，我把这个模式复用扩展到了列表页——只要当前页面里还有任意一条记录没到终态，就会每 5 秒静默刷新一次，全部到终态才停止，而且切换页面离开时会自动清理定时器。

## Slide 12：贡献 5 —— 测试用例文档与单元测试

> 第五个是测试相关的工作。我写了一份 test-cases.md，覆盖 14 个章节一共 96 个测试用例，同时主动标注了 5 处代码审查中发现的模糊行为，比如乐观锁冲突目前并没有被服务层检测到——这个我没有回避，而是如实记录下来。基于这份文档，我用 JUnit 5 加 Mockito 写了三个测试类：校验器测试、状态机测试、还有服务层的部分测试，一共 44 个测试方法，全部通过，而且完全不依赖真实数据库连接，也不受后台定时任务干扰。

---

## Slide 13：Live Demo 演示提纲

> 接下来我给大家演示一下实际效果。（按提纲现场操作，不需要逐字念稿，以下是操作要点）
> 1. 创建一笔新支付，展示表单校验和幂等键
> 2. 切换语言，展示界面文案和金额格式的联动变化
> 3. 打开详情页，展示状态自动从 CREATED 推进
> 4. 用余额不足的账户发起支付，展示强制转为 FAILED
> 5. 查看状态历史时间线
> 6. 演示回收站的软删除和恢复

## Slide 14：遇到的挑战与解决方案

> 开发过程中遇到几个比较典型的问题。第一个是后台定时任务和集成测试的冲突——如果测试直接用真实的 Spring 容器，后台任务会在测试运行期间抢先修改数据，导致断言随机失败，我们后来改成用 Mockito 做纯单元测试来规避这个问题。第二个是多语言切换后，组件库自带的文案没有跟着变，通过引入 el-config-provider 解决。第三个是随机失败模拟和真实余额校验之间的错误码可能冲突，我们明确了优先级——真实业务规则永远优先于演示用的随机逻辑。

## Slide 15：未来展望

> 如果还有更多时间，我们想做几件事：给 Controller 层补充测试，把整体测试覆盖率提到 80% 以上；解决回收站保留期边界测试缺少可控时间注入的问题；给乐观锁冲突场景补上真正的检测逻辑；多语言支持可以扩展更多语种；另外希望能接入真正的 CI/CD 流水线，把测试覆盖率检查做成自动化门禁。

## Slide 16：致谢

> 以上就是我负责的部分，谢谢大家聆听，欢迎提问。

---

## 备用问答准备（Q&A 预案）

以下是根据 [test-cases.md](test-cases.md) 中"待确认事项"整理的、评委可能追问的问题及应答要点：

**Q：为什么乐观锁版本冲突没有被处理？**
> 目前 `updateById` 的返回值确实没有被服务层检查，这是我们在测试阶段主动发现并记录下来的已知缺口，属于后续可以改进的点，而不是被忽略的 bug。

**Q：随机失败概率会不会和真实的余额校验冲突？**
> 不会，我们在代码里明确了优先级：余额校验发生在随机失败判断之前，真实业务规则始终优先。

**Q：为什么不做并发扣减？**
> 这是需求明确排除的范围——余额校验只是只读判断是否充足，不涉及真实扣款，所以天然不存在并发扣减的并发安全问题。

**Q：多语言里后端返回的错误信息也会跟着变吗？**
> 不会，后端 message 是固定语言，前端有一份独立的错误码字典优先展示，这是为了在不改后端代码的前提下尽量做到界面语言的一致性。
