# Payments Processing System 项目

[TOC]

## 概述

你的团队面临一个挑战：设计一个处理金融支付完整生命周期的支付处理页面。

系统将管理支付从创建到验证、处理，直至完成（或失败）的整个过程，并维护完整的状态变更 audit trail。

你的任务是构建这个页面。

## 技术目标

你应该致力于创建一个 Payments Processing REST API。这将是主要目标。

该 API 应支持创建、检索和跟踪支付的完整生命周期。


对于前端，你可以使用VUE框架。

前端应支持用户完成以下操作（按优先级排序）：

* 创建新的支付
* 查看支付状态和详情
* 查看支付历史（所有状态转换）
* 按状态搜索/筛选支付
* 查看失败支付的错误详情


## 支付生命周期

你的系统应实现以下支付工作流：

```
CREATED → VALIDATED → SENT → COMPLETED
                  ↓
              FAILED（可在任何阶段发生）
```

**Status 定义：**

* **CREATED** - 支付已提交，但尚未通过验证
* **VALIDATED** - 支付已通过所有验证规则，准备好发送
* **SENT** - 支付已传输到目标系统
* **COMPLETED** - 支付已成功处理并确认
* **FAILED** - 支付在流程中某个节点失败（附带错误码）

## 核心需求

### API Endpoint

自行设计 REST API 接口——决定哪些操作、路由和 HTTP 方法适用于创建、检索和跟踪支付的生命周期。

## 注意事项

1. **无需认证，假设只有单一用户，即不需要管理用户或账户归属**。

2. 你应该使用MySQL的数据库技术来进行持久化存储。

3. 充分利用 Git。尽可能使用分支和 Pull Request。

4. 任何关于如何使用 REST API 的文档都会很有用。如果你学过 Swagger/OpenAPI，可以尝试使用。

5. 对于此培训项目，你**不需要**对接真实的支付网络或网关——在内部模拟处理过程即可。

## 技术入门清单

1. 创建你的项目结构。

2. 创建 Git 仓库。

3. 将骨架项目 add、commit、push 到你的 Git 仓库。

4. 确保你的团队有Git仓库的访问权限。

5. 确定第一个可工作系统的绝对最小字段集——例如，第一版支付对象可能只需包含 ID、金额、货币和状态。


## 成功建议

1. 从小处着手。先让系统跑通，能够存储一个字段极少的简单支付。然后再逐步增强，存储更复杂的支付数据。

2. 尝试结对编程，效果可能非常显著。

3. 有意识地保持团队的良好氛围。例如：给团队起个名字，系统性地安排彼此之间的碰头会。

4. 质量优先于数量。

5. 尽早考虑 idempotency——如果某人提交了相同的支付两次，会发生什么？

6. 考虑如何处理状态转换——一个支付能从 COMPLETED 回到 CREATED 吗？（大概率不行！）

## 需要考虑的问题

### Idempotency
* 如果客户端提交了相同的支付两次，会发生什么？
* 重复的支付请求应该被拒绝，还是应该返回已有的支付？
* 如何检测重复？（考虑使用客户端提供的 idempotency key）

### 重试处理
* 网络调用可能失败——如何处理重试？
* 状态更新是否应该可重试？
* 在标记为 FAILED 之前应该重试多少次？

### 状态转换
* 定义合法的状态转换（例如，COMPLETED → SENT 是不合法的）
* 实现验证以防止不合法的转换
* 考虑使用 state machine 模式

### 故障场景下的 API 设计
* 如何向客户端传达错误？
* 不同错误类型应使用什么 HTTP 状态码？
* 是否应该返回客户端可以程序化使用的错误码？

### Audit Trail
* 每次状态变更都应记录时间戳
* 谁/什么触发了状态变更？
* 这对调试和合规性至关重要


## 附录 B：错误码示例

你的系统应为不同的失败场景定义清晰的错误码：

| Error Code                |              描述                | HTTP Status |
|---------------------------|---------------------------------------|-----|
| VALIDATION_FAILED         | 支付未通过验证检查                    | 400 |
| INSUFFICIENT_FUNDS        | 源账户余额不足                        | 400 |
| INVALID_ACCOUNT           | 账户号码不合法或不存在                | 400 |
| INVALID_CURRENCY          | 货币代码不受支持                      | 400 |
| INVALID_AMOUNT            | 金额为零、负数或不合法                | 400 |
| DUPLICATE_PAYMENT         | 具有相同 idempotency key 的支付已存在 | 409 |
| INVALID_STATUS_TRANSITION | 无法从当前状态转换到请求的状态        | 400 |
| PAYMENT_NOT_FOUND         | 支付 ID 不存在                        | 404 |
| PROCESSING_ERROR          | 支付处理过程中发生内部错误            | 500 |
| NETWORK_ERROR             | 与支付网络通信失败                    | 503 |

## 附录 C：验证规则示例

考虑实现以下验证规则：

1. **金额验证**
   - 金额必须大于 0
   - 金额不得超过上限（例如 1,000,000）
   - 大多数货币的金额最多保留 2 位小数

2. **账户验证**
   - 源账户和目标账户必须不同
   - 账户号码必须符合合法格式
   - 账户必须在系统中存在

3. **货币验证**
   - 货币代码必须是合法的 ISO 4217 代码（例如 USD、EUR、GBP）
   - 系统必须支持指定的货币

4. **状态转换验证**
   - 合法转换：
     - CREATED → VALIDATED
     - CREATED → FAILED
     - VALIDATED → SENT
     - VALIDATED → FAILED
     - SENT → COMPLETED
     - SENT → FAILED
   - 不合法的转换应被拒绝，并返回清晰的错误消息

## 附录 D：UI 创意

以下是一些 UI 概念，可能会给你一些灵感。

### 创建支付界面
* 包含以下字段的表单：
  - 源账户（下拉列表或文本输入）
  - 目标账户（文本输入）
  - 金额（数字输入）
  - 货币（下拉列表：USD、EUR、GBP 等）
  - 引用/备注（可选文本）
* 提交按钮
* 清晰的验证错误消息

### 支付详情界面
* 显示支付信息：
  - 支付 ID
  - 状态（颜色编码：绿色表示 COMPLETED，黄色表示进行中，红色表示 FAILED）
  - 金额和货币
  - 账户详情
  - 时间戳
* 状态历史时间线，显示所有转换
* 如果失败，显示错误码和描述

### 支付列表界面
* 支付表格或列表
* 列：支付 ID、金额、货币、状态、创建日期
* 按状态筛选（下拉列表或标签页）
* 按支付 ID 或引用搜索
* 点击行查看详情

### 状态历史可视化
* 时间线视图，显示：
  - 每个状态及其时间戳
  - 支付在每个状态中停留的时长
  - 任何备注或错误消息
  - 状态转换的视觉指示


## 附录 F：测试考虑事项

考虑以下测试场景：

1. **Happy Path**
   - 创建一个支付并观察它通过所有状态最终到达 COMPLETED

2. **验证失败**
   - 尝试创建金额为负数的支付
   - 尝试创建货币不合法的支付
   - 尝试创建源账户和目标账户相同的支付

3. **重复检测**
   - 使用相同的 idempotency key 提交相同的支付两次
   - 验证第二次请求返回已有的支付

4. **非法状态转换**
   - 尝试将 COMPLETED 的支付移回 CREATED
   - 验证返回适当的错误响应

5. **并发更新**
   - 两个请求同时尝试更新同一个支付
   - 验证数据一致性

6. **数据库故障模拟**
   - 处理过程中数据库不可用怎么办？
   - 状态更新是否应该 rollback？

## 附录 G：架构建议

考虑分层架构：

```
┌─────────────────────────────────────┐
│         Web UI (Frontend)           │
│             (Vue框架)               │
└──────────────┬──────────────────────┘
               │ HTTP/REST
┌──────────────▼──────────────────────┐
│         REST API Layer               │
│        (Spring Boot )                │
├──────────────────────────────────────┤
│         业务逻辑层                   │
│   - 支付验证                         │
│   - 状态转换逻辑                     │
│   - Idempotency 检查                 │
├──────────────────────────────────────┤
│         数据访问层                   │
│     (MyBatis plus )                 │
│                                     │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│            数据库                    │
│            (MySQL )                  │
└──────────────────────────────────────┘
```

**关键考虑因素：**
* 将关注点分离到不同的层
* 使业务逻辑可独立于数据库进行测试
* 使用 dependency injection 使组件松散耦合
* 为数据库操作实现适当的 transaction management
